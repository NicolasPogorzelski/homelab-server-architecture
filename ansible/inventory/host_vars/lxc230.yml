---
# Docker Compose stacks on this node. dest = project_src on the node;
# src = subdir under repo docker/ that docker-compose-update syncs from.
compose_projects:
  - { dest: /opt/openwebui, src: openwebui }
