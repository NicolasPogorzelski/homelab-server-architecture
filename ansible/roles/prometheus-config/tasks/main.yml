---
- name: deploy prometheus from template
  ansible.builtin.template:
    src: prometheus.yml.j2
    dest: /opt/homelab-server-architecture/docker/monitoring/prometheus/prometheus.yml
    owner: root
    group: root
    mode: '0644'
    lstrip_blocks: yes
  notify: reload prometheus-config
