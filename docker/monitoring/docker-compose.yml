services:
  prometheus:
    image: prom/prometheus:v3.12.0  # pinned to running version (2026-06-17)
    container_name: prometheus
    network_mode: host
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=${PROM_RETENTION}
      - --web.enable-lifecycle
      - --web.listen-address=${PROM_BIND_ADDRESS}:${PROM_PORT}
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/rules:/etc/prometheus/rules:ro
      - ${PROM_DATA}:/prometheus
    restart: unless-stopped
    logging:
      options:
        max-size: "10m"
        max-file: "3"

  grafana:
    image: grafana/grafana:13.0.2  # pinned to running version (2026-06-17)
    container_name: grafana
    network_mode: host
    env_file:
      - ${GF_ENV_FILE}
    volumes:
      - ${GF_DATA}:/var/lib/grafana
      - ./grafana/provisioning/datasources/prometheus.yml:/var/lib/grafana/provisioning/datasources/prometheus.yml:ro
    restart: unless-stopped
    logging:
      options:
        max-size: "10m"
        max-file: "3"

  alertmanager:
    image: prom/alertmanager:v0.32.2  # pinned to running version (2026-06-17)
    container_name: alertmanager
    network_mode: host
    command:
      - --config.file=/etc/alertmanager/alertmanager.yml
      - --storage.path=/alertmanager
      - --web.listen-address=${AM_BIND_ADDRESS}:${AM_PORT}
    volumes:
      - ./alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
    restart: unless-stopped
    logging:
      options:
        max-size: "10m"
        max-file: "3"

  node-exporter:
    image: prom/node-exporter:v1.11.1  # pinned to running version (2026-06-17)
    container_name: node-exporter
    network_mode: host
    pid: host
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--web.listen-address=${NE_BIND_ADDRESS}:${NE_PORT}'
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--path.rootfs=/rootfs'
    restart: unless-stopped
    logging:
      options:
        max-size: "10m"
        max-file: "3"

  blackbox-exporter:
    image: prom/blackbox-exporter:v0.28.0  # pinned to running version (2026-06-17)
    container_name: blackbox-exporter
    network_mode: host
    command:
      - '--config.file=/etc/blackbox_exporter/config.yml'
      - '--web.listen-address=${BB_BIND_ADDRESS}:${BB_PORT}'
    volumes:
      - ./blackbox/blackbox.yml:/etc/blackbox_exporter/config.yml:ro
    restart: unless-stopped
    logging:
      options:
        max-size: "10m"
        max-file: "3"
